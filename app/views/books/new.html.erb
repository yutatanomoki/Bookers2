<h1>New Book</h1>
<%= form_with model: @book, local:true do |f| %>
  
  <h4>Title</h4>
  <%= f.text_field :book_name %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>